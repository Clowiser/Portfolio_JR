import React from "react";
import {SGameContainer, SGameHeader, STitle} from "./Game.styled";

const Game = () => {

    return (
        <SGameContainer>
            <SGameHeader>
                <STitle>Game</STitle>
            </SGameHeader>
        </SGameContainer>
    )
}

export default Game;